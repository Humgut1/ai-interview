# AI 면접 도구

미리 정한 **평가 기준(rubric)** 에 따라 1차 면접을 진행하고, 점수와 **그렇게 판단한 근거**를 정리해 사람이 검토할 수 있게 해 주는 도구입니다.

> 합격 여부를 정하는 것은 언제나 사람입니다. AI 는 점수와 근거만 제시합니다.

## 어떻게 쓰나요

```
① 인사담당자가 직무와 평가 기준을 만든다
② 후보자에게 인터뷰 링크를 보낸다
③ 후보자가 링크를 열고 AI 와 채팅으로 면접을 본다
④ AI 가 기준대로 채점하고 근거가 된 답변을 함께 남긴다
⑤ 사람이 결과를 검토하고 최종 판단을 한다
```

## 설계 원칙

- **AI 는 합불을 결정하지 않는다.** 점수와 근거만 제시하고 판단은 사람이 한다.
- **모든 점수에는 근거가 된 답변 원문이 함께 저장된다.** 나중에 왜 그런 점수가 나왔는지 확인할 수 있어야 한다.
- **억양·유창성·성격은 평가하지 않는다.** rubric 에 적힌 내용만 평가한다.
- **후보자 화면은 불안감을 줄이는 방향으로 만든다.** 진행률 표시, 이어하기, 명확한 사전 안내.

## 진행 상황

| 단계 | 내용 | 상태 |
| --- | --- | --- |
| 1 | 평가 기준(rubric) 설정 화면 | ✅ 완료 (mock 데이터) |
| 2 | 후보자 인터뷰 채팅 화면 | ✅ 완료 (mock 데이터) |
| 3 | 결과 리뷰 리포트 화면 | ✅ 완료 (mock 데이터) |
| 4 | 화면 규칙(디자인) 정하고 전 화면에 적용 | ✅ 완료 (`/design`) |
| 5 | 대시보드 · 후보자 관리 | ✅ 완료 (mock 데이터) |
| 6 | Claude API 연결 ① 질문·평가 기준 초안 생성 | ✅ 완료 |
| 7 | Claude API 연결 ② 답변 채점 · 후속 질문 | ⬜ 다음 |
| 8 | Supabase 스키마 · 저장 연결 (`lib/store.ts`, `supabase/screen-001-tables.sql`) | 🟨 코드 완료 · 표 적용 대기 |

**화면을 먼저 만들고 AI 연결을 나중에 붙이고 있습니다.** 무엇을 보여 줄지가 정해져야 AI 에게 무엇을 시킬지도 정해지기 때문입니다. 6단계에서 첫 번째 AI 기능(질문 초안 생성)이 실제로 동작하게 됐고, 채점과 후속 질문은 아직 정해진 규칙대로만 움직입니다. 저장은 여전히 브라우저에만 됩니다.

## 화면 구성 (계획 포함)

| 경로 | 설명 | 접근 |
| --- | --- | --- |
| `/jobs/new` | 직무 · 평가 기준 만들기 | 관리자 |
| `/jobs/[id]/edit` | 평가 기준 수정 | 관리자 |
| `/jobs/[id]/candidates` | 후보자 관리, 인터뷰 링크 발급 | 관리자 |
| `/jobs/[id]/compare` | 후보자 점수 비교 | 관리자 |
| `/interview/[token]` | 후보자 인터뷰 진행 | 링크(토큰)만으로 접근 |
| `/interviews/[id]` | 결과 리포트 | 관리자 |
| `/dashboard` | 직무 목록 · 요약 통계 | 관리자 |
| `/design` | 화면 규칙(색 · 글자 · 버튼 · 점수 표시) | 내부 확인용 |

## 1단계에서 만든 것

`/jobs/new` 한 화면입니다.

- 직무명 · 직무 설명 입력
- 질문 추가 / 삭제 / 순서 변경 (드래그 또는 ↑↓ 버튼)
- 질문마다 **우수 · 보통 · 미흡** 세 단계의 답변 기준 작성
- 질문별 **비중** — 정수로 입력하면 전체 대비 %를 자동 계산(합계는 항상 100%)
- 질문별 **후속 질문 최대 횟수** (0~3회)
- **직무 설명으로 질문 초안 만들기** — Claude 가 직무 설명을 읽고 질문·기준·비중을 만들어 줍니다 (6단계에서 실제 연결)
- 저장 전 미입력 항목 검사, 임시저장(브라우저 보관) 및 이어서 작성

## 2단계에서 만든 것

`/interview/[token]` — 후보자가 링크만으로 들어와 진행하는 화면입니다. 로그인이 없습니다.

- **시작 전 안내 화면** — 질문 수, 예상 소요 시간, 진행 방식, 데이터 활용 동의 체크박스(동의해야 시작 가능)
- **채팅 진행** — 질문 하나씩, 진행률 표시, AI가 답변을 읽는 동안 로딩 표시
- **후속 질문** — 답변이 짧으면 정해 둔 횟수 안에서 더 구체적으로 되물음
- **제출 후 수정 불가** — 시작 전과 입력창 아래에 미리 안내
- **이어하기** — 창을 닫았다 같은 링크로 다시 들어오면 진행 상태가 그대로 복원됨
- **완료 화면** — 감사 인사와 다음 절차 안내

후보자 화면에는 **평가 기준(rubric)을 절대 내려보내지 않습니다.** 질문 문장만 전달합니다. 기준을 보고 답을 맞추는 일이 없어야 하기 때문입니다.

진행 상태는 지금 브라우저에 보관합니다(API 연결 단계에서 서버 저장으로 옮깁니다). 그래서 다른 기기에서 열면 이어하기가 되지 않습니다.

## 3단계에서 만든 것

`/interviews/[id]` — 담당자가 면접 결과를 검토하는 화면입니다.

- **종합 점수 + 한 줄 요약** — 질문 비중을 반영한 가중 평균
- **문항별 카드** — 질문 / 답변 전문 / AI 점수 / 채점 근거
- **근거가 된 발언 표시** — 점수의 이유가 된 문장을 따로 뽑아 주고, 답변 전문에서도 같은 문장을 형광펜으로 표시합니다. "왜 이 점수인지"를 원문에서 바로 확인할 수 있습니다.
- **담당자 점수 수정 + 메모** — AI 점수는 그대로 두고 수정값을 따로 보관합니다. 언제든 AI 점수로 되돌릴 수 있고, 종합 점수는 수정값 기준으로 다시 계산됩니다.
- **대면 면접용 추천 질문** — 문항마다 더 확인해 볼 질문
- **전체 트랜스크립트** — 오간 대화 전문
- **같은 직무 후보자 목록** — 일부러 점수순 정렬을 하지 않습니다. 줄을 세우는 순간 AI 점수가 합불처럼 읽히기 때문입니다.

이 화면은 담당자만 봅니다. 평가 기준(rubric)도 여기서만 보입니다. 검토 내용 저장은 아직 화면 동작만 있고, 실제 저장은 API 연결 단계에서 붙입니다.

## 4단계에서 만든 것 — 화면 규칙

세 가지 방향의 시안을 만들어 비교한 뒤 하나로 정했습니다. **빽빽한 뼈대 + 차분한 색** 조합입니다.

- **바탕은 살짝 따뜻한 회색, 카드는 흰색** — 파란 기운이 도는 회색보다 오래 봐도 덜 피곤합니다.
- **강조색은 진한 청록 하나뿐** — 화면에서 색이 들어간 곳은 "지금 눌러야 할 버튼"과 "근거가 된 문장" 두 가지뿐입니다.
- **선으로 나누고 그림자는 쓰지 않습니다** — 정보가 빽빽한 화면에서는 그림자가 겹쳐 지저분해집니다.
- **숫자는 전용 서체** — 점수가 88에서 100으로 바뀌어도 자리가 흔들리지 않습니다.
- **모서리는 6px** — 둥글수록 말랑해 보이고, 각질수록 딱딱해 보입니다. 그 중간입니다.

색과 크기 값은 `app/globals.css` 한 곳에 모아 두었습니다. 거기만 고치면 모든 화면이 함께 바뀝니다. 버튼 · 카드 · 라벨처럼 반복되는 부품은 `components/ui/styles.ts` 에 모아 두었습니다.

`/design` 은 이 규칙을 한 장에 모아 둔 **스타일 가이드**입니다. 새 화면을 만들 때 여기 있는 것만 골라 씁니다.

### 색으로 하지 않기로 한 것

- **등급에 신호등(초록 · 노랑 · 빨강)을 쓰지 않습니다.** 색만 보고 합불을 정하게 되기 때문입니다. 단계는 글자 진하기로만 구분합니다.
- **점수 막대는 몇 점이든 같은 색입니다.** 낮은 점수를 빨갛게 칠하는 순간, 담당자가 답변을 읽지 않게 됩니다.
- **후보자 목록은 지원한 순서대로 놓습니다.** 점수순으로 줄을 세우면 위에 있는 사람이 더 나은 사람처럼 보입니다.

### 결과 검토 화면 구조

담당자 화면은 세로로 세 칸으로 나눴습니다. 스크롤을 내려도 양쪽 칸은 자리를 지킵니다.

```
[ 후보자 목록 ] [ 문항별 채점 · 근거 · 답변 전문 ] [ 검토 현황 · 메모 · 저장 ]
```

후보자 화면(모바일)은 반대로 한 줄입니다. 질문은 흰 카드, 내 답변은 옅은 색 블록으로 오른쪽에 들여 씁니다. 사람과 대화하는 메신저처럼 보이지 않게 하려는 것입니다.

## 5단계에서 만든 것

`/dashboard` 와 `/jobs/[id]/candidates` — 담당자가 매일 여는 두 화면입니다.

### 대시보드 (`/dashboard`)

- **먼저 볼 사람부터** — 맨 위 네 칸은 검토 대기 · 진행 중 면접 · 제출 완료 · 진행 중 공고입니다. 그중 제일 왼쪽 **검토 대기**가 "답변은 다 냈는데 아직 아무도 열어 보지 않은 사람"입니다.
- **공고 목록** — 공고마다 후보자 수와 검토 대기 수를 함께 보여 주고, 누르면 그 공고의 후보자 관리로 넘어갑니다.

### 후보자 관리 (`/jobs/[id]/candidates`)

- **면접 링크 발급** — 누르면 후보자 한 줄이 생기고 링크가 만들어집니다. 링크에 들어가는 값은 순번(1, 2, 3…)이 아니라 **추측하기 어려운 무작위 값**입니다. 이 값 하나만 있으면 면접에 들어갈 수 있기 때문입니다.
- **링크 복사** — 브라우저가 복사를 막으면 링크를 화면에 그대로 펼쳐 직접 복사할 수 있게 합니다.
- **단계 거르기** — 링크 발급 / 진행 중 / 제출 완료 로 나눠 봅니다. 이 단계는 **후보자가 어디까지 왔는지**이고, 그 옆의 미검토 · 검토중 · 검토완료는 **담당자가 어디까지 봤는지**입니다. 두 가지는 다른 축이라 따로 표시합니다.
- **점수는 제출을 마친 사람에게만 보입니다.** 아직 진행 중인 사람 자리에는 "아직 없음"이라고만 적습니다.
- **목록은 지원한 순서** — 여기서도 점수순 정렬은 하지 않습니다.

후보자는 **실명 없이 익명 라벨(후보자 A, B, C…)** 로만 다룹니다. 링크 발급과 만료 처리는 API 연결 단계에서 서버가 맡습니다. 지금은 링크를 만들어도 브라우저 화면에만 남습니다.

## 6단계에서 만든 것 — 질문 생성에 Claude 연결

여기까지는 "AI 면접 도구"라고 하면서 정작 질문은 사람이 다 썼습니다. 초안 버튼도 **직무 설명을 읽지 않고** 미리 적어 둔 문장 3개를 돌려주는 흉내였습니다. 그 부분을 실제 Claude 호출로 바꿨습니다.

### 무슨 일이 일어나나요

```
직무 설명을 적는다
   → [직무 설명으로 질문 초안 만들기] 를 누른다
   → 우리 서버가 Claude 에게 그 설명을 보낸다
   → 질문 4~6개 + 우수/보통/미흡 기준 + 비중이 돌아온다
   → 사람이 읽고 고친다  ← 이 단계는 없앨 수 없습니다
```

### AI 에게 지켜 달라고 못박은 것

`lib/ai/draft-questions.ts` 에 규칙을 적어 두었습니다.

- **모든 후보자에게 똑같이 물을 수 있는 고정 질문**만 만듭니다. 사람마다 질문이 달라지면 점수를 나란히 놓고 볼 수 없습니다.
- **지식 퀴즈가 아니라 해 본 일을 묻습니다.** ("~을 아시나요" 대신 "~했던 경험을 말씀해 주세요")
- **직무 설명에 없는 역량은 지어내지 않습니다.**
- **채점 기준은 인상이 아니라 관찰 가능한 내용으로 씁니다.** ("열정적이다" ✗ / "본인이 한 행동과 결과를 사실로 뒷받침한다" ○)
- **직무와 무관한 개인 정보는 묻지 않습니다.** 나이 · 성별 · 결혼 · 출산 · 가족 · 출신 지역 · 출신 학교 · 외모 · 재산 · 종교 · 정치 성향 · 병역 · 건강/장애. 질문에도, 채점 기준에도 넣지 못하게 했습니다.

### 형식이 어긋나지 않게 하는 장치

AI 가 문장으로 답하면 화면이 못 읽습니다. 그래서 **답의 모양을 미리 정해 두고**(질문 텍스트 / 비중 1~10 / 후속 질문 0~3회 / 기준 세 줄) 그 모양으로만 답하게 합니다. 돌아온 값도 그대로 믿지 않고 서버에서 한 번 더 확인해, 비어 있거나 범위를 벗어난 것은 걸러 냅니다.

### 키는 브라우저로 내려가지 않습니다

Claude 키(`ANTHROPIC_API_KEY`)는 **서버에만** 둡니다. 브라우저는 우리 서버의 `/api/questions/draft` 주소에 부탁만 하고, 키는 구경도 하지 못합니다. 키가 브라우저에 있으면 화면 소스를 열어 본 누구나 우리 키로 요금을 쓸 수 있습니다.

키를 넣지 않고 이 저장소를 받아 실행하면, 버튼은 **예시 초안**을 대신 넣고 "지금 것은 직무 설명을 반영하지 않은 예시"라고 화면에 밝힙니다. 그 밖에 키가 틀렸을 때 · 요청이 몰렸을 때 · 형식이 어긋났을 때도 각각 무엇이 잘못됐는지 한국어로 알려 줍니다.

## 실행 방법

```bash
npm install
npm run dev
```

브라우저에서 http://localhost:3000 을 엽니다.

환경변수는 `.env.example` 을 복사해 `.env.local` 로 만들어 채웁니다. 실제 키가 담긴 `.env.local` 은 git 에 올라가지 않습니다.

```bash
cp .env.example .env.local
```

질문 초안 생성을 실제로 쓰려면 `.env.local` 의 `ANTHROPIC_API_KEY=` 뒤에 본인 키를 붙여 넣고 `npm run dev` 를 다시 실행합니다. 키는 [console.anthropic.com](https://console.anthropic.com) 에서 발급합니다. **키를 넣지 않아도 화면은 전부 돌아갑니다** — 초안 버튼만 예시 문장으로 대체됩니다.

## 폴더 구조

```
app/                        화면(페이지)
  globals.css               색 · 글자 값 (여기만 고치면 전 화면이 바뀜)
  page.tsx                  홈
  dashboard/page.tsx        대시보드 (공고 목록 · 요약)
  jobs/new/page.tsx         직무 · 평가 기준 만들기
  jobs/[id]/candidates/page.tsx  후보자 관리 · 링크 발급
  interview/[token]/page.tsx 후보자 인터뷰
  interviews/[id]/page.tsx  결과 리포트
  design/page.tsx           스타일 가이드(화면 규칙)
  api/questions/draft/route.ts  직무 설명 → 질문 초안 (서버에서만 Claude 호출)
components/
  rubric/                   RubricBuilder, QuestionEditor
  interview/                ChatWindow, ConsentScreen, MessageBubble,
                            AnswerInput, ProgressBar, CompleteScreen
  review/                   ReportView, ScoreCard, TranscriptView,
                            CandidateList
  candidates/               CandidateManager (후보자 목록 · 링크 발급)
  ui/                       Field(입력 필드), TopBar(공통 머리띠),
                            styles.ts(공통 부품 모음)
lib/
  ai/                       Claude 호출 (client.ts: 키·모델, draft-questions.ts: 초안 생성)
  types.ts                  Job / Question / Criteria / 인터뷰 · 리포트 타입
  rubric.ts                 순서 변경, 비중 계산, 예상 시간, 유효성 검사
  interview.ts              인터뷰 진행 규칙 (다음 질문 / 후속 질문 / 종료)
  interview-store.ts        진행 상태 보관 (이어하기)
  review.ts                 종합 점수 계산, 등급 환산, 근거 문장 찾기
  candidates.ts             단계별 집계, 단계 거르기, 링크 토큰 만들기
  mock/                     화면 확인용 가짜 데이터
```

## 기술 스택

Next.js (App Router) · TypeScript · Tailwind CSS · Claude API(질문 초안 생성에 연결됨) · Supabase(서버에서만 service_role 로 접근)

외부 UI 라이브러리는 쓰지 않고, 데이터는 ORM 없이 Supabase 를 직접 조회할 예정입니다.
